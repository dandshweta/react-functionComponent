import { useState } from "react";
const Demo = () => {
  const { inputcounter, outputcounter } = useState(0);

  const onBtnClick = () => {
    setCounter(counter + 1);
    setCounter(counter + 1);
    setCounter(counter + 1);
    setCounter(counter + 1);
  };
};
export default Demo;
